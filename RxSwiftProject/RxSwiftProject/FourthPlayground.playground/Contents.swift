import Foundation
import RxSwift
import RxCocoa

// Filtering Operators
// ignoreElements - Only completed events, next elements ignored
// elementAt(position)
// filter
// skip(count)
// skipWhile { predicate }
// take(count)
// takeWhile {predicate}
// skipUntil
// takeUntil
// distinctUntilChanged


// Transforming operators
// map
// flatMap
// flatMapLatest - combination of map and switchLatest



// Combining operators
// startWith(values)
// concat(a, b)
// a.concat(b)
// merge()
// combineLatest(a, b)
// zip(a, b)
// switchLatest() - flatmapLatest without the map
// a.amb(b)
// reduce(0, accumulator: {})
// scan(0, accumulator: {})
